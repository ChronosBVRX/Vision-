version: '3.8'

services:
  vision-plus:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vision-plus
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - NODE_ENV=production
